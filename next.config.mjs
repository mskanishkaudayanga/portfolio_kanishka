/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['assets.aceternity.com', 'images.unsplash.com','cloud.appwrite.io','res.cloudinary.com'],
  },
};

export default nextConfig;